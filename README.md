# Norways Diff Checker

An Electron desktop app shell styled after Hazeron Starship Studio. The workspace is intentionally blank for now.

## Run

```powershell
npm install
npm start
```

Run `npm run check` to check the JavaScript syntax.
